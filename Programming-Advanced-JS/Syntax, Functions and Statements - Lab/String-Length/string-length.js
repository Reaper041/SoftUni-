function stringLength(first, second, third) {
    let total = first.length + second.length + third.length;
    let everage = Math.round(total / 3);

    console.log(total);
    console.log(everage);
}
